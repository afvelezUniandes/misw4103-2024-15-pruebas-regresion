{
  "scripts": {
    "cypress:run": "cypress run",
    "backstop:reference": "npx backstop reference",
    "backstop:test": "npx backstop test",
    "test:full": "npm run cypress:run && npm run backstop:test"
  },
  "dependencies": {
    "cypress": "^13.15.2"
  }
}
